'use client';

import { PagePlaceholder } from '@/components/page-placeholder';
import { Store } from 'lucide-react';

export default function PartnersPage() {
  return (
    <PagePlaceholder
      title="Parceiros"
      description="Gerencie parceiros e comodatos"
      icon={Store}
    />
  );
}
